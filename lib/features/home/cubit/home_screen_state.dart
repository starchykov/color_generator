part of 'home_screen_cubit.dart';

/// Represents the state of the HomeScreen.
///
/// This is an immutable class that should contain all the properties
/// that might change in response to application events or user actions
/// on the HomeScreen. Since it's immutable, all changes require a new
/// instance of [HomeScreenState].
@immutable
class HomeScreenState {
  // Properties related to the home screen state would be defined here.

  // Initializer and methods for updating the state would also be defined here.
}
