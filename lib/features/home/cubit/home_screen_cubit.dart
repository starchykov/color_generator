import 'package:flutter/cupertino.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

part 'home_screen_state.dart';

/// A Cubit that manages the state of the HomeScreen.
///
/// This Cubit starts with an initial [HomeScreenState] and
/// should contain business logic for updating the state as the user
/// interacts with the HomeScreen.
class HomeScreenCubit extends Cubit<HomeScreenState> {
  /// Initializes the Cubit with an initial [HomeScreenState].
  HomeScreenCubit() : super(HomeScreenState());

// Business logic for the HomeScreen should be added here.
// TODO(starchykov): Replace it with your business logic.
}
